apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Release.Namespace }}-sync-dir
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 2Gi
  hostPath:
    path: {{ .Values.oneshot.host.syncDir }}
    type: ""
  persistentVolumeReclaimPolicy: Retain
  volumeMode: Filesystem
