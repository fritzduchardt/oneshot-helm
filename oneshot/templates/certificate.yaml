apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: oneshot-certificate
spec:
  dnsNames:
    - {{ .Values.nginx.ingress.host | quote }}
  duration: 2160h
  issuerRef:
    kind: ClusterIssuer
    name: {{ .Values.nginx.ingress.clusterIssuer }}
  renewBefore: 360h
  secretName: {{ .Values.nginx.ingress.tlsSecretName | quote }}
  subject:
    organizations:
      - friclu