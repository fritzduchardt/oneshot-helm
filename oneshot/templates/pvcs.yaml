apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sync-dir
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: ""
  volumeMode: Filesystem
  volumeName: {{ .Release.Namespace }}-sync-dir