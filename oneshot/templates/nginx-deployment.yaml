apiVersion: apps/v1
kind: Deployment
metadata:
  name: oneshot-nginx
  labels:
    app.kubernetes.io/name: oneshot
    app.kubernetes.io/component: nginx
spec:
  replicas: {{ .Values.nginx.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: oneshot
      app.kubernetes.io/component: nginx
  template:
    metadata:
      labels:
        app.kubernetes.io/name: oneshot
        app.kubernetes.io/component: nginx
    spec:
      containers:
        - name: nginx
          image: {{ include "oneshot.image" (dict "image" .Values.nginx.image "tag" .Values.nginx.tag "Values" .Values) }}
          ports:
            - containerPort: {{ .Values.nginx.service.targetPort }}
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: default.conf
      volumes:
        - name: nginx-config
          configMap:
            name: oneshot-nginx